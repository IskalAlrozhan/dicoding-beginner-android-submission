<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ValorantAgent" parent="Theme.Material3.DayNight">
        <item name="background">@color/valCol</item>
        <item name="android:statusBarColor">@color/valCol</item>
        <item name="android:textColor">@color/white</item>

        <item name="colorPrimary">@color/valCol</item>
        <item name="colorSecondary">@color/valCol</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnSurface">@color/white</item>
    </style>

</resources>